-- Dashboard
INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'dashboard', 'Dashboard', '/super-admin/dashboard', 'dashboard', 1
FROM Modules
WHERE ModuleKey = 'super_admin';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'user_access', 'User & Access', NULL, 'manage_accounts', 2
FROM Modules WHERE ModuleKey = 'user_access';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'operations', 'Operations', NULL, 'business_center', 3
FROM Modules WHERE ModuleKey = 'printing';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'academic', 'Academic Operations', NULL, 'school', 4
FROM Modules WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'communication', 'Communication', NULL, 'campaign', 5
FROM Modules WHERE ModuleKey = 'communication';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'reports', 'Reports & Analytics', '/reports', 'analytics', 6
FROM Modules WHERE ModuleKey = 'reports';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'hr', 'HR Management', '/hr', 'groups', 7
FROM Modules WHERE ModuleKey = 'hr_management';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'system_tools', 'System & Tools', NULL, 'construction', 8
FROM Modules WHERE ModuleKey = 'workflow_engine';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, NULL, 'system_control', 'System Control', NULL, 'settings', 9
FROM Modules WHERE ModuleKey = 'system_control';