groups

CREATE TABLE PermissionGroups (
    PermissionGroupId INT IDENTITY(1,1) PRIMARY KEY,
    GroupKey NVARCHAR(100) NOT NULL UNIQUE,
    GroupName NVARCHAR(150) NOT NULL,
    Description NVARCHAR(255) NULL,
    SortOrder INT NOT NULL DEFAULT 0,
    CreatedAt DATETIME NOT NULL DEFAULT GETDATE()
);


ALTER TABLE Permissions
ADD PermissionGroupId INT NULL;

ALTER TABLE Permissions
ADD CONSTRAINT FK_Permissions_PermissionGroups
FOREIGN KEY (PermissionGroupId)
REFERENCES PermissionGroups(PermissionGroupId);

INSERT INTO PermissionGroups (GroupKey, GroupName, Description, SortOrder)
VALUES
('super_admin', 'Super Admin Control', 'Module, menu, button, widget and system control permissions', 1),
('user_access', 'User & Access', 'Users, roles, access levels and permission management', 2),
('printing', 'Printing Management', 'Photocopy request, approval, queue and report permissions', 3),
('inventory', 'Inventory Management', 'Paper inventory, purchase and transaction permissions', 4),
('it_service_desk', 'IT Service Desk', 'Ticketing and support permissions', 5),
('it_assets', 'IT Asset Management', 'Asset, assignment and maintenance permissions', 6),
('academic', 'Academic Operations', 'Academic year, terms, students, classrooms and rota permissions', 7),
('end_term', 'End-of-Term Campaigns', 'Term 1, Term 2 and Term 3 campaign permissions', 8),
('observations', 'Teacher Observations', 'Teacher observation and feedback permissions', 9),
('communication', 'Communication Center', 'Email, notification and announcement permissions', 10),
('reports', 'Reports & Analytics', 'Dashboard, report and export permissions', 11),
('hr', 'HR Management', 'Future HR module permissions', 12),
('foundation', 'Foundation Tools', 'Workflow, forms, documents, calendar, tasks, archive, search and security permissions', 13);

-- Super Admin / System Control
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'super_admin')
WHERE PermissionKey LIKE 'SuperAdmin.%'
   OR PermissionKey LIKE 'Module.%'
   OR PermissionKey LIKE 'Menu.%'
   OR PermissionKey LIKE 'Button.%'
   OR PermissionKey LIKE 'Widget.%'
   OR PermissionKey LIKE 'FeatureFlag.%'
   OR PermissionKey LIKE 'SystemSettings.%'
   OR PermissionKey LIKE 'AuditLog.%'
   OR PermissionKey LIKE 'Backup.%';

-- User & Access
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'user_access')
WHERE PermissionKey LIKE 'User.%'
   OR PermissionKey LIKE 'Role.%'
   OR PermissionKey LIKE 'Permission.%'
   OR PermissionKey LIKE 'UserOverride.%'
   OR PermissionKey LIKE 'AccessLevel.%';

-- Printing
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'printing')
WHERE PermissionKey LIKE 'Printing.%'
   OR PermissionKey LIKE 'Request.%';

-- Inventory
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'inventory')
WHERE PermissionKey LIKE 'Inventory.%';

-- IT Service Desk
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'it_service_desk')
WHERE PermissionKey LIKE 'Ticket.%';

-- IT Assets
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'it_assets')
WHERE PermissionKey LIKE 'Asset.%';

-- Academic
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'academic')
WHERE PermissionKey LIKE 'Academic.%'
   OR PermissionKey LIKE 'AcademicYear.%'
   OR PermissionKey LIKE 'Term.%'
   OR PermissionKey LIKE 'Student.%'
   OR PermissionKey LIKE 'StudentID.%'
   OR PermissionKey LIKE 'Classroom.%'
   OR PermissionKey LIKE 'Homeroom.%'
   OR PermissionKey LIKE 'YearLeader.%'
   OR PermissionKey LIKE 'DutyRota.%';

-- End Term
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'end_term')
WHERE PermissionKey LIKE 'EndTerm.%';

-- Observations
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'observations')
WHERE PermissionKey LIKE 'Observation.%';

-- Communication
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'communication')
WHERE PermissionKey LIKE 'Communication.%'
   OR PermissionKey LIKE 'Email.%'
   OR PermissionKey LIKE 'Notification.%'
   OR PermissionKey LIKE 'Announcement.%';

-- Reports
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'reports')
WHERE PermissionKey LIKE 'Reports.%';

-- HR
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'hr')
WHERE PermissionKey LIKE 'HR.%';

-- Foundation Tools
UPDATE Permissions
SET PermissionGroupId = (SELECT PermissionGroupId FROM PermissionGroups WHERE GroupKey = 'foundation')
WHERE PermissionKey LIKE 'Workflow.%'
   OR PermissionKey LIKE 'FormBuilder.%'
   OR PermissionKey LIKE 'Document.%'
   OR PermissionKey LIKE 'Calendar.%'
   OR PermissionKey LIKE 'Task.%'
   OR PermissionKey LIKE 'KnowledgeBase.%'
   OR PermissionKey LIKE 'Archive.%'
   OR PermissionKey LIKE 'GlobalSearch.%'
   OR PermissionKey LIKE 'Security.%';