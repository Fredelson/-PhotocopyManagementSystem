INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 4, 'printing_management', 'Printing Management',
'/printing', 'print', 1
FROM Modules
WHERE ModuleKey = 'printing';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 4, 'inventory_management', 'Inventory Management',
'/inventory', 'inventory_2', 2
FROM Modules
WHERE ModuleKey = 'inventory';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 4, 'it_service_desk', 'IT Service Desk',
'/it-service-desk', 'support_agent', 3
FROM Modules
WHERE ModuleKey = 'it_service_desk';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 4, 'it_asset_management', 'IT Asset Management',
'/it-assets', 'devices', 4
FROM Modules
WHERE ModuleKey = 'it_assets';

-- ============================================
-- ACADEMIC OPERATIONS CHILD MENUS
-- ParentMenuId = 5
-- ============================================

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'academic_years', 'Academic Years',
'/academic/years', 'event', 1
FROM Modules
WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'terms', 'Terms',
'/academic/terms', 'calendar_month', 2
FROM Modules
WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'students', 'Students',
'/academic/students', 'groups', 3
FROM Modules
WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'student_ids', 'Student IDs',
'/academic/student-ids', 'badge', 4
FROM Modules
WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'classrooms', 'Classrooms',
'/academic/classrooms', 'meeting_room', 5
FROM Modules
WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'homeroom_teachers', 'Homeroom Teachers',
'/academic/homeroom-teachers', 'person_pin', 6
FROM Modules
WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'year_leaders', 'Year Leaders',
'/academic/year-leaders', 'supervisor_account', 7
FROM Modules
WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'duty_rota', 'Teacher Duty Rota',
'/academic/duty-rota', 'assignment_ind', 8
FROM Modules
WHERE ModuleKey = 'academic_operations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'teacher_observations_menu', 'Teacher Observations',
'/observations', 'assignment', 9
FROM Modules
WHERE ModuleKey = 'teacher_observations';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 5, 'end_term_campaigns_menu', 'End-of-Term Campaigns',
'/end-term-campaigns', 'event_note', 10
FROM Modules
WHERE ModuleKey = 'end_term_campaigns';

-- ============================================
-- COMMUNICATION CHILD MENUS
-- ParentMenuId = 6
-- ============================================

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 6, 'emailing_system', 'Emailing System',
'/communication/emailing', 'email', 1
FROM Modules
WHERE ModuleKey = 'communication';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 6, 'email_templates', 'Email Templates',
'/communication/email-templates', 'mark_email_read', 2
FROM Modules
WHERE ModuleKey = 'communication';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 6, 'email_queue', 'Email Queue',
'/communication/email-queue', 'outbox', 3
FROM Modules
WHERE ModuleKey = 'communication';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 6, 'email_logs', 'Email Logs',
'/communication/email-logs', 'receipt_long', 4
FROM Modules
WHERE ModuleKey = 'communication';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 6, 'notifications', 'Notifications',
'/communication/notifications', 'notifications', 5
FROM Modules
WHERE ModuleKey = 'communication';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 6, 'notification_rules', 'Notification Rules',
'/communication/notification-rules', 'rule', 6
FROM Modules
WHERE ModuleKey = 'communication';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 6, 'announcements', 'Announcements',
'/communication/announcements', 'campaign', 7
FROM Modules
WHERE ModuleKey = 'communication';

-- ============================================
-- SYSTEM & TOOLS CHILD MENUS
-- ParentMenuId = 9
-- ============================================

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'workflow_engine_menu', 'Workflow Engine',
'/workflow-engine', 'account_tree', 1
FROM Modules
WHERE ModuleKey = 'workflow_engine';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'form_builder_menu', 'Form Builder',
'/form-builder', 'dynamic_form', 2
FROM Modules
WHERE ModuleKey = 'form_builder';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'document_management_menu', 'Document Management',
'/documents', 'folder', 3
FROM Modules
WHERE ModuleKey = 'document_management';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'calendar_scheduling_menu', 'Calendar & Scheduling',
'/calendar', 'calendar_month', 4
FROM Modules
WHERE ModuleKey = 'calendar_scheduling';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'task_center_menu', 'Task Center',
'/tasks', 'task_alt', 5
FROM Modules
WHERE ModuleKey = 'task_center';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'knowledge_base_menu', 'Knowledge Base',
'/knowledge-base', 'menu_book', 6
FROM Modules
WHERE ModuleKey = 'knowledge_base';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'archive_center_menu', 'Archive Center',
'/archive', 'archive', 7
FROM Modules
WHERE ModuleKey = 'archive_center';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'global_search_menu', 'Global Search',
'/search', 'search', 8
FROM Modules
WHERE ModuleKey = 'global_search';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 9, 'security_center_menu', 'Security Center',
'/security', 'security', 9
FROM Modules
WHERE ModuleKey = 'security_center';

-- ============================================
-- SYSTEM CONTROL CHILD MENUS
-- ParentMenuId = 10
-- ============================================

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 10, 'module_manager', 'Module Manager',
'/system-control/modules', 'view_module', 1
FROM Modules
WHERE ModuleKey = 'system_control';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 10, 'menu_manager', 'Menu Manager',
'/system-control/menus', 'menu_open', 2
FROM Modules
WHERE ModuleKey = 'system_control';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 10, 'button_manager', 'Button Manager',
'/system-control/buttons', 'smart_button', 3
FROM Modules
WHERE ModuleKey = 'system_control';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 10, 'widget_manager', 'Widget Manager',
'/system-control/widgets', 'dashboard_customize', 4
FROM Modules
WHERE ModuleKey = 'system_control';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 10, 'feature_flags', 'Feature Flags',
'/system-control/feature-flags', 'toggle_on', 5
FROM Modules
WHERE ModuleKey = 'system_control';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 10, 'audit_logs', 'Audit Logs',
'/system-control/audit-logs', 'history', 6
FROM Modules
WHERE ModuleKey = 'system_control';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 10, 'backup_restore', 'Backup & Restore',
'/system-control/backup', 'backup', 7
FROM Modules
WHERE ModuleKey = 'system_control';

INSERT INTO Menus (ModuleId, ParentMenuId, MenuKey, MenuName, Route, Icon, SortOrder)
SELECT ModuleId, 10, 'system_settings', 'System Settings',
'/system-control/settings', 'settings', 8
FROM Modules
WHERE ModuleKey = 'system_control';